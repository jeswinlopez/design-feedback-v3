# Production build-time config for Vite (read by `vite build`).
# These are CLIENT-SAFE values: the publishable/anon key is designed to be embedded in
# the browser bundle, and Row Level Security (not key secrecy) protects the data. The
# service role key must NEVER appear here. You can instead set these as Vercel project
# env vars — actual env vars override this file at build time.
VITE_SUPABASE_URL=https://malnyatixnjtuzfswkfu.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_LL7YOZaeIMpMqq9QMLodkg_Rs6ikdji
